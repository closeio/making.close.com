import React from 'react';
import { Link } from 'gatsby';

const Navigation = () => {
  return (
    <nav className="navigation">
      <Link to="/">Home</Link>
    </nav>
  );
};

export default Navigation;
